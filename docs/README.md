# docs/

The static GitHub Pages site. Generated by `python py/main.py docs` from the
files in `output/`; nothing here is edited by hand except the vendored
front-end assets under `vendor/`.

Built in step S4 of the work plan — see `PRIMER.md`, part C.
