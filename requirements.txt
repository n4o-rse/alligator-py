# Pinned once the versions have actually been installed and run (PRIMER A3).
# Core:
rdflib==7.1.1
numpy==2.1.3
pandas==2.2.3

# Figures (step S2). Pillow is matplotlib's JPEG writer and is pinned in its own
# right: the SVG is byte-stable across versions, the JPEG is not, so an
# unpinned Pillow would turn a rebuild into a diff (PRIMER A3, A4).
matplotlib==3.10.8
pillow==12.1.1

# Optional extra [amt] — the AMT.engine binding of step S6. Deliberately not
# listed here: it is a git dependency pinned to a commit, and a pin like that
# belongs in pyproject.toml rather than in a file of version numbers. Install
# it on top of this one with
#
#     pip install -e ".[amt]"
#
# which brings the engine and its own three dependencies (rdflib, pyshacl,
# pyvis). Without it the pipeline is complete except for the amt phase.
